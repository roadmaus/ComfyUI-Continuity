"""LTX 2.5 — Lightricks' 22B audio-video DiT, the pack's second video family.

Nothing is imported here. `models.py` and `sampling.py` are pure declarations
the manifest and the suites read without a server; `render.py` pulls in
ComfyUI and is reached only through `registry.video("ltx25")`.
"""
