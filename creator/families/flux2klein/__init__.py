"""Flux 2 Klein — BFL's compact Flux 2, as a still-image family.

Deliberately empty: `declare.py` must be importable without pulling anything
of the family's in, and the registry imports submodules by name.
"""
